# TODO

## Python parity (next steps)
- Add handover manager (cross-version active detection + handover_* messaging).
- Add async throttling queue to mimic worker behavior more closely.
- Mirror produced API endpoints (for @uns-kit/api parity).
